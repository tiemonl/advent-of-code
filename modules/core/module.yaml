product:
  type: lib
  platforms: [jvm]

dependencies:
  - $libs.kotlin.gradle.plugin: exported
  - $libs.kotlin.stdlib: exported
  - $kotlin.reflect

settings:
  kotlin:
    languageVersion: 2.2
    serialization: enabled