product:
  type: lib
  platforms: [jvm]

dependencies:
  - $libs.kotlin.gradle.plugin: exported
  - $libs.kotlin.stdlib: exported
  - $kotlin.reflect
  - $libs.okio
test-dependencies:
  - $libs.okio.fakefilesystem

apply:
  - ../../templates/testing.module-template.yaml

settings:
  kotlin:
    languageVersion: 2.2
    serialization: enabled